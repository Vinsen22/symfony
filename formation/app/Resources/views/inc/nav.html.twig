{% block body %}
    <div class="container">
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">

                </div>
                <ul class="nav navbar-nav">
                    <li class="active"><a href = "{{ url('contact.form') }}">Accueil</a></li>
                    <li><a href = "{{ url('contact') }}">Liste des Contacts</a></li>

                </ul>
            </div>
        </nav>
        {# <img src="{{ asset('img/logo.png') }}" alt="">#}
    </div>
{% endblock %}